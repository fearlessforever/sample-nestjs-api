import { AnotherExceptionFilter } from './another-exception.filter';

describe('AnotherExceptionFilter', () => {
  it('should be defined', () => {
    expect(new AnotherExceptionFilter()).toBeDefined();
  });
});
